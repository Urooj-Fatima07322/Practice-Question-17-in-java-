# Practice-Question-17-in-java-
This Java practice program demonstrates abstraction and polymorphism using an abstract `BankAccount` class. It calculates interest for `SavingsAccount` and `CurrentAccount` objects and performs deposit operations through a common interface.
